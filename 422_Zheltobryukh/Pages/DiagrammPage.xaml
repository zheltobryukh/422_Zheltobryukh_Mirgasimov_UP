<Page x:Class="_422_Zheltobryukh.Pages.DiagrammPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:_422_Zheltobryukh.Pages"
      xmlns:charts="clr-namespace:System.Windows.Forms.DataVisualization.Charting;assembly=System.Windows.Forms.DataVisualization"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="DiagrammPage">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <TextBlock Text="Пользователь:" VerticalAlignment="Center" Margin="5"/>
            <ComboBox x:Name="CmbUser" Width="200" Margin="5"
                      DisplayMemberPath="FIO"
                      SelectionChanged="UpdateChart"/>

            <TextBlock Text="Тип диаграммы:" VerticalAlignment="Center" Margin="15,5,5,5"/>
            <ComboBox x:Name="CmbDiagram" Width="150" Margin="5"
                      SelectionChanged="UpdateChart"/>

            <Button x:Name="ButtonExcel" Content="Excel" Width="100" Margin="15,5,5,5"
                    Click="ButtonExcel_Click" ToolTip="Экспорт в Excel"/>
            <Button x:Name="ButtonWord" Content="Word" Width="100" Margin="5"
                    Click="ButtonWord_Click" ToolTip="Экспорт в Word"/>
        </StackPanel>

        <WindowsFormsHost Grid.Row="1" Margin="10">
            <charts:Chart x:Name="ChartPayments">
                <charts:Chart.Legends>
                    <charts:Legend/>
                </charts:Chart.Legends>
            </charts:Chart>
        </WindowsFormsHost>
    </Grid>
</Page>