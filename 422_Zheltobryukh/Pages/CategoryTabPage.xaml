<Page x:Class="_422_Zheltobryukh.Pages.CategoryTabPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="CategoryTabPage">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="80" />
        </Grid.RowDefinitions>

        <DataGrid x:Name="DataGridCategory" Grid.Row="0" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>

                <DataGridTextColumn Header="Категория" Binding="{Binding Name}" Width="300"/>
                <DataGridTemplateColumn Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>

                            <Button x:Name="ButtonEdit" Content="Редактировать" Click="ButtonEdit_Click" ToolTip="Редактировать данные"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <Button x:Name="ButtonAdd" Grid.Row="1" Content="Добавить" HorizontalAlignment="Left" Width="100" Margin="10" Click="ButtonAdd_Click" ToolTip="Добавить данные"/>

        <Button x:Name="ButtonDel" Grid.Row="1" Content="Удалить" HorizontalAlignment="Right" Width="100" Margin="10" Click="ButtonDel_Click" ToolTip="Удалить данные"/>
    </Grid>
</Page>